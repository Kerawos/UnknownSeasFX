package pl.mareksowa.models.maps;

/**
 * Holder for equivalent of 'denger' level of world map. RED means that on this field player can find very tough adventures,
 * YELLOW will be neutral and GREEN is focused for merchants.
 */
public enum FieldColor {
    GREEN, YELLOW, RED
}
